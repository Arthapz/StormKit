#pragma once

#define EDO_VERSION "${EDO_MAJOR_VERSION}.${EDO_MINOR_VERSION}.${EDO_PATCH_VERSION}-${EDO_GIT_BRANCH}(${EDO_GIT_COMMIT_HASH})"
#define EDO_MAJOR_VERSION ${EDO_MAJOR_VERSION}
#define EDO_MINOR_VERSION ${EDO_MINOR_VERSION}
#define EDO_PATCH_VERSION  ${EDO_PATCH_VERSION}
#define EDO_BUILD_TYPE "${EDO_BUILD_TYPE}"
#define EDO_GIT_BRANCH "${EDO_GIT_BRANCH}"
#define EDO_GIT_COMMIT_HASH "${EDO_GIT_COMMIT_HASH}"
