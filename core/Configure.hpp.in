#pragma once

#define STORM_VERSION "${STORM_MAJOR_VERSION}.${STORM_MINOR_VERSION}.${STORM_PATCH_VERSION}-${STORM_MILESTONE}-${STORM_GIT_BRANCH}(${STORM_GIT_COMMIT_HASH})"
#define STORM_MAJOR_VERSION ${STORM_MAJOR_VERSION}
#define STORM_MINOR_VERSION ${STORM_MINOR_VERSION}
#define STORM_PATCH_VERSION  ${STORM_PATCH_VERSION}
#define STORM_MILESTONE "${STORM_MILESTONE}"
#define STORM_BUILD_TYPE ${STORM_BUILD_TYPE}
#define STORM_GIT_BRANCH "${STORM_GIT_BRANCH}"
#define STORM_GIT_COMMIT_HASH "${STORM_GIT_COMMIT_HASH}"
